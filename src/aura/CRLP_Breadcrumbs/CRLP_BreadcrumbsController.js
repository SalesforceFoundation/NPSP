({
    doInit: function(cmp, event, helper){
        /**
         * TODO: once the new pages and logic are created
         !v.labels.return "/apex/STG_SettingsManager"
         !v.labels.rollupSummaryTitle "/apex/CRLP_Setup"
         !v.labels.filterGroupsTitle /apex/CRLP_FilterGroups?
         */
    }
})