class tokenizedGift {

    constructor(amount, firstName, lastName, currencyCode, token) {
        this.amount = amount;
        this.firstName = firstName;
        this.lastName = lastName;
        this.currencyCode = currencyCode;
        this.token = token;
    }

}

export default tokenizedGift;