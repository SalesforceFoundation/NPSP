<apex:page controller="STG_PanelMakeDefaultAllocations_CTRL" >
    <apex:form id="form" styleClass="slds-m-around--x-large">
        <c:STG_PageHeader sectionLabel="{!$Label.stgNavBulkProcesses}" pageLabel="{!$Label.alloBatchCreateDefault}" />
        <p class="slds-text-body--small slds-m-top--x-small"><apex:outputText value="{!$Label.alloBatchDefaultInfo}" escape="false" /></p>
        <c:UTIL_PageMessages />
        <div class="slds-grid slds-grid--align-center slds-grid--vertical-align-center slds-p-around--large">
            <apex:commandButton value="{!$Label.stgBtnRunBatch}" status="statusLoad" action="{!runBatch}" disabled="{!NOT(stgService.stgAllo.Default_Allocations_Enabled__c)||ISNULL(stgService.stgAllo.Default__c)||isRunningBatch}" immediate="true" rerender="form" styleClass="slds-button slds-button--small slds-button--neutral" />
        </div>
        <apex:outputPanel rendered="{!isRunningBatch}">
            <c:UTIL_JobProgressLightning id="jobProgress"
                eventTargetId="{!$Component.jobProgress}"
                strBatchComponentLabel="{!$Label.stgLabelAlloBatchProgress}"
                cNumberOfJobs="1"
                startPolling="True"
                stopPollingOnComplete="True"
                pollingDelay="1000"/>
        </apex:outputPanel>
    </apex:form>
<!--
                <div class="col-sm-offset-1 col-sm-10">
                    <apex:outputPanel rendered="{!isReadOnlyMode&&NOT(isRunningBatch)}" layout="block" styleClass="panel panel-default">
                        <div class="panel-heading">
                            <h3 class="panel-title">{!$Label.alloBatchCreateDefault}</h3>
                        </div>
                        <div class="panel-body" style="padding:15px;">
                            <apex:outputText value="{!$Label.alloBatchDefaultInfo}" escape="false" />
                            <div style="text-align:center;" class="slds-form-element__control">
                                <apex:commandButton value="{!$Label.stgBtnRunBatch}" status="statusLoad" action="{!runBatch}" disabled="{!NOT(stgService.stgAllo.Default_Allocations_Enabled__c)||ISNULL(stgService.stgAllo.Default__c)}" immediate="true" rerender="form" styleClass="slds-button slds-button--small slds-button--neutral" rendered="{!isReadOnlyMode}"/>
                            </div>
                        </div>
                    </apex:outputPanel>
                    <apex:outputPanel styleClass="slds-container--large slds-container--center" rendered="{!isRunningBatch}">
                        <c:UTIL_JobProgressLightning id="jobProgress"
                            eventTargetId="{!$Component.jobProgress}"
                            strBatchComponentLabel="{!$Label.stgLabelAlloBatchProgress}"
                            cNumberOfJobs="1"
                            startPolling="False"
                            stopPollingOnComplete="True"
                            pollingDelay="1000"/>
                    </apex:outputPanel>
                </div>	-->
</apex:page>