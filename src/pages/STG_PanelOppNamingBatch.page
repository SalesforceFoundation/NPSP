<apex:page controller="STG_PanelOppNamingBatch_CTRL" >
    <c:STG_PageHeader sectionLabel="{!$Label.stgNavBulkProcesses}" pageLabel="{!$Label.stgNavRollupBatch}" />
    <p>{!$Label.stgHelpBatchOppRollup}</p>

    <apex:form id="form" styleClass="slds-m-around--x-large">
        <c:UTIL_PageMessages />
        <apex:pageBlock id="pbRelBatch" mode="maindetail">
            <div class="slds-button-group">
                <apex:commandButton value="{!$Label.stgBtnRunBatch}" status="statusLoad" action="{!runBatch}" disabled="{!isRunningBatch}" immediate="true" rerender="pbRelBatch" styleClass="slds-button slds-button--small slds-button--neutral" />
            </div>

            <apex:outputPanel rendered="{!isRunningBatch}">
                <c:UTIL_JobProgress strBatchComponentLabel="{!$Label.stgLabelOppNamingRefreshTitle}" cNumberOfJobs="4" bRerenderOnComplete="true" />
            </apex:outputPanel>

        </apex:pageBlock>
    </apex:form>

        <!-- Batch Opportunity Naming --
        <div class="col-sm-offset-1 col-sm-10" style="margin-top:50px">
            <apex:outputPanel rendered="{!isReadOnlyMode && NOT(IsRunningBatch) && listOppNameSettings.size>0}" layout="block" styleClass="panel panel-danger">
                <div class="panel-heading">
                    <div class="panel-title">{!$Label.stgLabelOppNamingRefreshTitle}</div>
                </div>
                <div class="panel-body" style="padding:15px;">
                    <apex:outputText value="{!$Label.stgHelpBtnOppNaming}" escape="false" />
                    <div style="text-align:center;" class="slds-form-element__control">
                        <apex:commandbutton value="{!$Label.stgBtnRefreshOpportunityNames}" status="statusLoad" action="{!runBatch}" rendered="{!isReadOnlyMode}" immediate="false" rerender="form" styleClass="btn slds-button slds-button--small slds-button--destructive"/>
                    </div>
                </div>
            </apex:outputPanel>
        </div>
        <!-- Batch Progress Bar --
        <div class="col-sm-offset-1 col-sm-10" style="margin-top:10px">
            <apex:outputPanel rendered="{!isRunningBatch}">
                <br/><br/>
                <c:UTIL_JobProgress strBatchComponentLabel="{!$Label.stgLabelOppNamingRefreshTitle}" cNumberOfJobs="1" bRerenderOnComplete="true" />
                <br/>
            </apex:outputPanel>
        </div> -->	
</apex:page>